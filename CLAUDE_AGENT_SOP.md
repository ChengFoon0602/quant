# 量化因子自动挖掘 (Factor Mining) Agent SOP

## 目标与定位
通过引入 QuantSkills 中 FactorMAD (Factor Multi-Agent Debate) 与 Loop Evolve 的理念，将 AI 的定位从“报告代写员/代码辅助”升级为**“自动化量化矿工”**。AI 需利用本地完善的向量化回测引擎和正交化工具，自主完成因子闭环发现。

---

## 触发词：`/mine_factors`

当用户在对话中发送 `/mine_factors`（或明确要求挖掘新因子）时，本 Agent 必须不经询问，自动进入以下 **“生成-正交-回测-反思” (G-O-B-R)** 循环工作流：

### Step 1: 因子假设生成 (Generation)
1. **输入限制**：查阅本地缓存管线提供的量价/财务字段，禁止凭空捏造字段。
2. **第一性原理**：不随机组合算子。必须先阐述因子的**物理含义**或**微观交易行为学逻辑**（例如：“散户恐慌抛售引起的过度反应缩量”、“大单压盘下的小单主动吃货”）。
3. **公式化**：将逻辑转换为数学公式，**优先调用** `signals/alpha191_ext.py` 中的高性能算子 (`ts_argmax`, `decay_linear` 等)。

### Step 2: 剥离去噪与代码化 (Orthogonalization)
1. **编写代码**：将设计的因子以纯 Pandas 向量化形式写入 `signals/auto_mined_alpha.py`（严禁 `for` 循环遍历股票）。
2. **强制正交化**：在将新因子送入回测引擎前，**必须调用** `signals/orthogonalize.py` 中的 `cross_sectional_orthogonalize()`，将新因子相对于 `size`（市值）、`volatility`（波动率）或 `alpha012`（现有主力因子）进行施密特正交剥离。
3. **防御机制**：确保所有截面排序和归一化处理没有偷偷引入 $t+1$ 时刻的未来均值。

### Step 3: 黑盒沙盒测试 (Backtesting)
1. 自动调用本地的评估脚本（如 `backtest/ic_evaluator.py` 与 `report.py`）。
2. **等待引擎输出**：在终端执行回测命令并抓取 stdout 日志，读取该正交化残差因子的 Rank IC，IC IR 以及叠加双边严格摩擦成本（0.026% / 0.076%）后的资金曲线。

### Step 4: 审判与反思 (Reflection)
作为严苛的质控官，基于测试结果进行内部审判：
- 💀 **判定死亡 (Discard)**：如果 Rank IC 均值 $< 0.03$、或多周期 IC 衰减过快（lag_3 已降为 0）、或扣费后夏普比率 $< 1.0$。执行动作：**删除该因子代码**，在内部日志中记录逻辑缺陷（如：噪声过大、被市值因子完全吸收）。
- 🌟 **判定存活 (Keep)**：若正交化后仍具备显著的正 IC 且夏普大于 1.5。执行动作：保留代码，并向用户正式汇报。

---

## Agent 红线约束
1. **绝对禁止**：在测试新因子时去改动 `backtest/engine.py` 的扣费口径和 `W[t-1] * ret[t]` 的收益对齐公式来“迎合”结果。
2. **宁缺毋滥**：如果连续 3 次闭环生成的因子均失效，停止循环并向用户求助，不交垃圾结果。
